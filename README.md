# otari-anyguardrails-container
Container for using any-guardrails from otari

Basic Python container files included:
- `Dockerfile`
- `requirements.txt`
- `app.py`

Build and run:
```bash
docker build -t otari-anyguardrails-container .
docker run --rm otari-anyguardrails-container
```
